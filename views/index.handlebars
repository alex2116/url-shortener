<div class="container" style="box-shadow:0 4px 12px 0; height: 430px; border-radius: 5px">
  <div style="text-align: center; margin-top: 30px; padding: 30px;">    
    <h4>URL Shortener</h4>
    <i class="fas fa-link fa-5x mt-4"></i>
  </div>  
  {{#if input}}{{>input}}{{/if}}
  {{#if result}}{{>result}}{{/if}}
  {{>errorMessage}}
</div>